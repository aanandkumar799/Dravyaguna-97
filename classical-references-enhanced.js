(()=>{
  const $=id=>document.getElementById(id);const list=$('list'),info=$('info'),search=$('search');if(!list||!info||!search)return;
  const clean=v=>String(v??'').trim(),arr=v=>Array.isArray(v)?v.filter(x=>x!==null&&x!==undefined&&clean(x)!==''):[],name=p=>p.name||p.identity?.name||p.classical_sanskrit_name||p.id,sans=p=>p.sanskrit_name||p.identity?.sanskrit_name||'',bot=p=>p.botanical_name||p.identity?.botanical_name||'';
  const esc=v=>clean(v).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\\':'&#039;','"':'&quot;'}[c]||c));let plants=[];
  function refs(p){const c=p.classical_reference||{};const out=[];arr(c.shlokas).forEach(x=>{if(typeof x==='object'){const value=x.text||x.shloka||'';if(value||x.reference||x.source)out.push({kind:'Shloka',value,source:x.source||x.reference||'Classical source',extra:[x.transliteration,x.meaning,x.chapter].filter(Boolean).join(' • '),verified:x.verified===true})}else out.push({kind:'Shloka',value:String(x),source:'Classical source',extra:'',verified:false})});arr(c.nighantu_references).forEach(x=>out.push({kind:'Nighantu',value:typeof x==='object'?(x.reference||x.text||JSON.stringify(x)):String(x),source:'Nighantu reference',extra:'',verified:false}));arr(c.samhita_references).forEach(x=>out.push({kind:'Samhita',value:typeof x==='object'?(x.reference||x.text||JSON.stringify(x)):String(x),source:'Samhita reference',extra:'',verified:false}));return out}
  function render(){const q=clean(search.value).toLowerCase();const rows=[];plants.forEach(p=>refs(p).forEach(r=>{const hay=[name(p),sans(p),bot(p),r.kind,r.value,r.source,r.extra].join(' ').toLowerCase();if(!q||hay.includes(q))rows.push({p,r})}));info.textContent=`${rows.length} reference item${rows.length===1?'':'s'} shown across ${plants.length} NCISM plants`;if(!rows.length){list.innerHTML='<div class="empty">No classical reference record found for this search.</div>';return}list.innerHTML=rows.map(({p,r})=>`<article class="card"><h2>#${esc(p.order||'')} — ${esc(name(p))}</h2><div class="meta">${esc(sans(p))}${bot(p)?' • '+esc(bot(p)):''}</div><div class="block"><div class="label">${esc(r.kind)} • ${esc(r.source)}${r.verified?' • ✓ verified':''}</div><div class="shloka">${esc(r.value||'Reference recorded without shloka text.')}</div>${r.extra?`<div class="meta">${esc(r.extra)}</div>`:''}</div><a class="link" href="plant.html?id=${encodeURIComponent(p.id)}">Open Plant Dossier →</a></article>`).join('')}
  Promise.all([fetch('plant-index.json',{cache:'no-store'}).then(r=>r.json()),fetch('plants.json',{cache:'no-store'}).then(r=>r.json())]).then(([idx,data])=>{const index=Array.isArray(idx)?idx:idx.plants||[],full=Array.isArray(data)?data:data.plants||[],fm=new Map(full.map(p=>[p.id,p]));plants=index.filter(p=>p&&p.category==='NCISM-97'&&Number(p.order)>=1&&Number(p.order)<=97).sort((a,b)=>Number(a.order)-Number(b.order)).map(p=>({...p,...(fm.get(p.id)||{})}));render();search.oninput=render}).catch(()=>{info.textContent='Unable to load the classical reference database.';list.innerHTML='<div class="empty">Reference database unavailable.</div>'});
})();